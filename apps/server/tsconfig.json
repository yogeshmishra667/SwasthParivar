{
  "//": "Editor + typecheck + ESLint project-service config. Includes tests so type-aware lint rules can resolve them. The build (pnpm build) uses tsconfig.build.json instead to keep dist/ src-only.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2022"],
    "types": ["node"],
    "noEmit": true
  },
  "include": ["src/**/*", "tests/**/*"],
  "exclude": ["dist", "node_modules"],
  "references": [
    { "path": "../../packages/shared-types" },
    { "path": "../../packages/domain-logic" },
    { "path": "../../packages/test-factories" }
  ]
}
